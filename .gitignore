# ============ 密钥与本地配置（严禁提交）============
# 真实视觉 API 配置（含 API Key），GitHub 只保留 vision_config.env.example
skill/config/vision_config.env

# Claude Code 本地个人设置（可能包含模型配置与密钥）
.claude/settings.local.json

# ============ Python ============
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# ============ 测试与覆盖率 ============
.pytest_cache/
.coverage
htmlcov/

# ============ 运行时产物（本项目不写这些，纯防御）============
# 临时图片与预处理副本只落在系统 TEMP，不会进项目目录；
# 下面几条是防止误操作把临时产物提交进去
*.log
/tmp/
/logs/

# ============ 系统文件 ============
.DS_Store
Thumbs.db
desktop.ini
